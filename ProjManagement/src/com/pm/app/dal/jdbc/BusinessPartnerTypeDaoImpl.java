/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.pm.app.dal.jdbc;

import com.pm.app.dal.dao.*;
import com.pm.app.dal.factory.*;
import com.pm.app.dal.dto.*;
import com.pm.app.dal.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class BusinessPartnerTypeDaoImpl extends AbstractDAO implements BusinessPartnerTypeDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT idbusiness_partner_type, type FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( idbusiness_partner_type, type ) VALUES ( ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET idbusiness_partner_type = ?, type = ? WHERE idbusiness_partner_type = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE idbusiness_partner_type = ?";

	/** 
	 * Index of column idbusiness_partner_type
	 */
	protected static final int COLUMN_IDBUSINESS_PARTNER_TYPE = 1;

	/** 
	 * Index of column type
	 */
	protected static final int COLUMN_TYPE = 2;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 2;

	/** 
	 * Index of primary-key column idbusiness_partner_type
	 */
	protected static final int PK_COLUMN_IDBUSINESS_PARTNER_TYPE = 1;

	/** 
	 * Inserts a new row in the business_partner_type table.
	 */
	public BusinessPartnerTypePk insert(BusinessPartnerType dto) throws BusinessPartnerTypeDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT, Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			stmt.setInt( index++, dto.getIdbusinessPartnerType() );
			stmt.setString( index++, dto.getType() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdbusinessPartnerType( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new BusinessPartnerTypeDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the business_partner_type table.
	 */
	public void update(BusinessPartnerTypePk pk, BusinessPartnerType dto) throws BusinessPartnerTypeDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getIdbusinessPartnerType() );
			stmt.setString( index++, dto.getType() );
			stmt.setInt( 3, pk.getIdbusinessPartnerType() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new BusinessPartnerTypeDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the business_partner_type table.
	 */
	public void delete(BusinessPartnerTypePk pk) throws BusinessPartnerTypeDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdbusinessPartnerType() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new BusinessPartnerTypeDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the business_partner_type table that matches the specified primary-key value.
	 */
	public BusinessPartnerType findByPrimaryKey(BusinessPartnerTypePk pk) throws BusinessPartnerTypeDaoException
	{
		return findByPrimaryKey( pk.getIdbusinessPartnerType() );
	}

	/** 
	 * Returns all rows from the business_partner_type table that match the criteria 'idbusiness_partner_type = :idbusinessPartnerType'.
	 */
	public BusinessPartnerType findByPrimaryKey(int idbusinessPartnerType) throws BusinessPartnerTypeDaoException
	{
		BusinessPartnerType ret[] = findByDynamicSelect( SQL_SELECT + " WHERE idbusiness_partner_type = ?", new Object[] {  new Integer(idbusinessPartnerType) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the business_partner_type table that match the criteria ''.
	 */
	public BusinessPartnerType[] findAll() throws BusinessPartnerTypeDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY idbusiness_partner_type", null );
	}

	/** 
	 * Returns all rows from the business_partner_type table that match the criteria 'idbusiness_partner_type = :idbusinessPartnerType'.
	 */
	public BusinessPartnerType[] findWhereIdbusinessPartnerTypeEquals(int idbusinessPartnerType) throws BusinessPartnerTypeDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE idbusiness_partner_type = ? ORDER BY idbusiness_partner_type", new Object[] {  new Integer(idbusinessPartnerType) } );
	}

	/** 
	 * Returns all rows from the business_partner_type table that match the criteria 'type = :type'.
	 */
	public BusinessPartnerType[] findWhereTypeEquals(String type) throws BusinessPartnerTypeDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE type = ? ORDER BY type", new Object[] { type } );
	}

	/**
	 * Method 'BusinessPartnerTypeDaoImpl'
	 * 
	 */
	public BusinessPartnerTypeDaoImpl()
	{
	}

	/**
	 * Method 'BusinessPartnerTypeDaoImpl'
	 * 
	 * @param userConn
	 */
	public BusinessPartnerTypeDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "constructions_web.business_partner_type";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected BusinessPartnerType fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			BusinessPartnerType dto = new BusinessPartnerType();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected BusinessPartnerType[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			BusinessPartnerType dto = new BusinessPartnerType();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		BusinessPartnerType ret[] = new BusinessPartnerType[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(BusinessPartnerType dto, ResultSet rs) throws SQLException
	{
		dto.setIdbusinessPartnerType( rs.getInt( COLUMN_IDBUSINESS_PARTNER_TYPE ) );
		dto.setType( rs.getString( COLUMN_TYPE ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(BusinessPartnerType dto)
	{
	}

	/** 
	 * Returns all rows from the business_partner_type table that match the specified arbitrary SQL statement
	 */
	public BusinessPartnerType[] findByDynamicSelect(String sql, Object[] sqlParams) throws BusinessPartnerTypeDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new BusinessPartnerTypeDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the business_partner_type table that match the specified arbitrary SQL statement
	 */
	public BusinessPartnerType[] findByDynamicWhere(String sql, Object[] sqlParams) throws BusinessPartnerTypeDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new BusinessPartnerTypeDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
